server:
  port: 9090

mybatis-plus:
  global-config:
    db-config:
      id-type: AUTO


spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/oj?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver